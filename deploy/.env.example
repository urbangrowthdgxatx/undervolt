# LiteLLM Gateway Configuration
# Copy to .env and fill in values
#
# Generate a master key: python3 -c "import secrets; print('sk-admin-' + secrets.token_hex(16))"

LITELLM_MASTER_KEY=sk-admin-CHANGE_ME
NVIDIA_NIM_API_KEY=nvapi-CHANGE_ME

# Model to warmup on boot
VLLM_MODEL_NAME=nemotron-3-nano
